Set up SprintSpends for this developer. Run this single command to install (or update) and configure:

```bash
curl -fsSL https://raw.githubusercontent.com/sellmaai/sprintspend/main/install.sh | bash
```
